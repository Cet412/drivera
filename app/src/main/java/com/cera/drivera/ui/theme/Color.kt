package com.cera.drivera.ui.theme

import androidx.compose.ui.graphics.Color

// --- DRIVERA CANVAS & SURFACE TOKENS ---
val DriveraBackground = Color(0xFF121212) // Main background (Anti-Glare)
val DriveraSurface = Color(0xFF2E2D2D)    // Card components / Placeholder skeleton
val DriveraAccentBlue = Color(0xFF1E40AF) // Lower deck curve element (Deep Blue)

// --- SEMANTIC STATUS TOKENS ---
val DmsSuccess = Color(0xFF16A34A)        // ON Status / System Active / Safe
val DmsWarning = Color(0xFFD97706)        // Mild distraction warning (Standard)
val DmsError = Color(0xFFDC2626)          // Critical condition / Drowsy / Alarm

// --- TYPOGRAPHY & ICON TOKENS ---
val TextPrimary = Color(0xFFD6D6D6)       // "DRIVERA" title text (Subtle light gray)
val TextOnBrand = Color(0xFFFFFFFF)       // High contrast for icons inside buttons